server:
  port: 9091

spring:
  application:
    name: engine
  cloud:
    config:
      name: ecommerce
  config:
    import: optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, httptrace, info, metrics